<Page x:Class="Darts_matches.MatchInputPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Darts_matches"
        mc:Ignorable="d"
        Title="MatchInputWindow" >
    <Grid Name="MatchInputGrid" KeyDown="OnKeyDown" Focusable="True" Loaded="MatchInputGridOnLoaded">
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Margin" Value="10,10,10,10" />
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Label Grid.Column="1" Grid.Row="0"  Content="Vul hier de naam van de wedstrijd in" HorizontalContentAlignment="Center" FontSize="20"/>
        <Button Grid.Column="1" Grid.Row="1" Content="Volgende" Click="PlayerInputPage" Background="#0078D7" Foreground="White" Padding="280,20,280,20" BorderThickness="0" Margin="10, 60, 10, 20" FontSize="20"/>
    </Grid>
</Page>
